import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    card: {
      borderRadius: '15px',
      height: '100%',
    },
    title : {
        margin: '0px, 100px',
        color: 'red',
    }
  });